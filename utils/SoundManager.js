// Silent variant to allow builds while fix compatibility
class SoundManager {
    playClick() { /* Silent */ }
    playError() { /* Silent */ }
    playSuccess() { /* Silent */ }
}

export default new SoundManager();
